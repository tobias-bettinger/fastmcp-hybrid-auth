# FastMCP Server Boilerplate Requirements
# Python 3.10+

# Core MCP Framework
fastmcp>=0.2.0

# HTTP client for Keycloak integration
httpx>=0.27.0

# JWT token handling
PyJWT>=2.8.0

# Optional: Redis for production token storage
# Uncomment if using Redis for persistent token storage
# key-value-py[redis]>=0.1.0
# redis>=5.0.0

# Cryptography for token encryption
cryptography>=42.0.0

# Azure authentication (included with fastmcp, but explicitly listed for clarity)
# msal>=1.28.0

# Additional utilities
python-dotenv>=1.0.0  # For .env file support
